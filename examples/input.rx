print("=== user interaction demo ===")

// get user input using ifu from sc library
name = ifu("what's your name? ")
age = ifu("how old are you? ")

age_num = age

if (age_num >= 18) {
    print("hello adult", name)
} diff {
    print("hello young", name)
}

print("name type:", typeof(name))
print("age type:", typeof(age))
print("age_num type:", typeof(age_num))
